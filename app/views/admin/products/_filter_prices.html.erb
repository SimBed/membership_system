<%= form_with(url: product_path(@product), method: "get") do |form| %>
   <%= form.select :product_period, options_for_select(@months, session[:product_period]), {class: "sort"}, {onchange: "this.form.requestSubmit()"} %>
<% end %>