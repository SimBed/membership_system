<%= turbo_stream.update "manage-account-#{@account_holder.id}" do %>
  <%= render partial: 'admin/clients/manage_account', locals: {client: @account_holder} %>
<% end %>
<turbo-stream action="prepend" target="flash">
  <template>
    <%= render_flash %> 
  </template>
</turbo-stream>