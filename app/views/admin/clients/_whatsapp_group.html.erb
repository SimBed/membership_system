<%= link_to image_tag('whatsapp.png',
                      class: ["table_icon",("greyed-out" unless client.whatsapp_group?)].compact.join(' ')),
                      client_path(client, whatsapp_group: !client.whatsapp_group?),
                      data: { "turbo-method": :patch } %>

