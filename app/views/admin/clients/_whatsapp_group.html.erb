<turbo-frame id="whatsapp-group-<%= client.id  %>">
  <%= link_to image_tag('whatsapp.png',
                        class: ["table_icon",("waiver" unless client.whatsapp_group?)].compact.join(' ')),
                        admin_client_path(client, whatsapp_group: !client.whatsapp_group?),
                        data: { "turbo-method": :patch } %>
</turbo-frame>

