<turbo-frame id="waiver-<%= client.id  %>">
  <%= link_to image_tag('waiver.png',
                        class: ["table_icon",("waiver" unless client.waiver?)].compact.join(' ')),
                        admin_client_path(client, waiver: !client.waiver?),
                        data: { "turbo-method": :patch } %>
</turbo-frame>
