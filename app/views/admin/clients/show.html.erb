  <h2><%= @client.name %> <form action=<%= "/admin/clients/#{params[:id]}" %> method="GET" >
      <select name="purchaseid" onchange="this.form.submit()" class= "sort">
        <%#= options_for_select @months, session[:attendance_period] %>
        <%= options_for_select @products_purchased, session[:purchaseid] %>
      </select>
  </form>
  </h2>

<div id="client-summary" class="container px-1 mb-5 margin-override">
  <%= render partial: 'client_summary' %>
</div>

<div id="attendances-table" class="container px-1 mb-5 margin-override">
  <%= render partial: 'client_attendances_table' %>
</div>

<%= link_to 'Back', admin_clients_path %>
