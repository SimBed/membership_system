<%= form_with(model: other_service, data: {turbo: false}) do |form| %>
  <% if other_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(other_service.errors.count, "error") %> prohibited this other_service from being saved:</h2>

      <ul>
        <% other_service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
