<div class="container px-1">
  <h4>Employee Accounts <%= link_to image_tag('add.png', class: "button_icon"),
                          new_employee_account_path,
                          data: { turbo_frame: dom_id(Account.new) } %>
  </h4>
  <%= turbo_frame_tag Account.new %>    
</div>
<%= turbo_frame_tag 'accounts' do %>
  <%= render 'accounts_table' %>
<% end %>