<%= form_with(url: employee_accounts_path, method: :post, class: 'admin_form pt-4 my-2') do |form| %>
<%#= form_with(model: [:admin, client], data: {turbo: false}) do |form| %>
  <% if account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>
      <ul>
        <% account.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%# temporary %>
  <%= form.hidden_field :role_id, value: Role.find_by(name: 'junioradmin').id %>
  <div class="field">
    <%= form.label :email, class: "form-mandatory" %>
    <%= form.text_field :email, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit @account.new_record? ? 'add account' : 'update account' %>
  </div>

  <div class="cancel">
    <%= link_to image_tag('delete.png', class: "table_icon"), @form_cancel_link %>
  </div>  
<% end %>
