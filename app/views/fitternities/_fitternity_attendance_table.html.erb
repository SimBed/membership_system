<div class ="d-flex bg-light w-50">
    <div class ="cell fw-bold">Attendances</div>
</div>
  <% @attendances.each_with_index do |a, index| %>
     <div class='d-flex bg-light w-50'>
       <div class='cell c-purchases-attendances-index'>
         <%= @attendances.size - index %>
       </div>
       <div class='cell'>
         <%= a.date %>
       </div>
     </div>
  <% end %>
